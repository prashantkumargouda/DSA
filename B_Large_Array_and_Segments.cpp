#include <bits/stdc++.h> 
using namespace std ; 

/*
    Large Array and Segments 
    n , k , x ; 

    so there is a and b 
    a = n elements 
    b = n * k elements 

    given an element x ,find no of positions l for which there is r 
    find r >= l for which the sum is atleast x 
*/
int main() {
    int t ; 
    cin >> t ; 

    while( t-- ) {
        int n , k , x ; 
        cin >> n >> k >> x ; 

        vector<int> a ; 
        for(auto &it : a) cin >> it ; 

        
    }
}